import { EntityScreenSkeleton } from "@/features/registration/components/entity-table-states";

export default function DashboardEventResponsesLoading() {
  return <EntityScreenSkeleton withToolbar />;
}
